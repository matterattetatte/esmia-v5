---
eleventyNavigation:
  key: "Start"
  order: 1
  url: "/#"
title: "Start"
---
{% block content %}
  {% set navItems = collections.all | eleventyNavigation | sort(false, false, "order") %}
{% for item in navItems | head(6) %}
    <section id="{{ item.key | slug }}">
      {% if item.key != "Start" %}
        {% set entry = collections.all | findByUrl(item.url) %}
        {% if entry %}
          {{ entry.templateContent | safe }}
        {% else %}
          <p>Fel: Innehåll kunde inte hittas för URL {{ item.url }}</p>
        {% endif %}
      {% else %}
<style>{% include "css/banner.css" %}</style>
<div class="banner-section">
  <div class="banner-image">
    <img src="/images/background.jpg" alt="Spansk cateringillustration" />
  </div>
</div>

  <div class="banner-text">
    <h1>UPPLEV <strong>SPANSKA SMAKER</strong> MED PASSION</h1>
    <p>Esmia HB erbjuder autentiska spanska cateringupplevelser, där varje rätt berättar en historia av tradition, kvalitet och kärlek till mat. Vi skapar minnesvärda ögonblick för dina evenemang med smaker från Spanien, tillagade med färska råvaror och hjärta.</p>
  </div>

      {% endif %}
    </section>
  {% endfor %}

{% endblock %}