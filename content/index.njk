---

---
<style>{% include "css/banner.css" %}</style>
<section class="banner">
<div class="banner-section">
  <div class="banner-image">
    <img src="/images/background.jpg" fetchpriority="high" alt="Spansk cateringillustration" />
  </div>
</div>

  <center class="banner-text">
    <h1>ESMIA HB</h1>
    <p>Spansk paella-och tapascatering i Stockholm</p>
  </center>
</section>

<section class="flex" style="flex-wrap: wrap;">
<div style="flex: 1;">
  <h2>Välkommen till ESMIA HB!</h2>
  <p>Vi erbjuder autentisk spansk catering med fokus på paella och tapas. Oavsett om du planerar en företagsfest, bröllop eller privat tillställning, är vi här för att göra ditt evenemang minnesvärt med våra läckra rätter och professionella service.</p>
</div>
<div style="min-width: 50%">
  <h2>Följ oss på Facebook!</h2>
  <p>Se våra senaste uppdateringar på sociala medier.</p>
<div class="sk-ww-facebook-page-posts" data-embed-id="25624561"></div>
</div>

</section>
<div>
<script>
// Preload your placeholder image early
new Image().src = '/images/iframe-profile.webp';

document.addEventListener('DOMContentLoaded', () => {
  // 1. Load the Facebook widget script only now
  const script = document.createElement('script');
  script.src = '/scripts/iframe-widget.js';
  script.defer = true;           // still respectful
  document.head.appendChild(script);

  // 2. Replace Facebook profile/post images as soon as they appear
  const replaceImages = () => {
    document.querySelectorAll('img[alt="sk-header-picture"], img[alt="sk-post-userpic"]')
      .forEach(img => {
        if (!img.src.includes('/images/iframe-profile.webp')) {
          img.src = '/images/iframe-profile.webp';
          img.srcset = ''; // kill any Facebook srcset
        }
      });
  };

  // Run immediately (in case widget already injected something)
  replaceImages();

  // And keep running whenever new content is added (Facebook loads async)
  const observer = new MutationObserver(replaceImages);
  observer.observe(document.body, { childList: true, subtree: true });
});
</script>

</div>